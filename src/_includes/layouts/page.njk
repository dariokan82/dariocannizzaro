---
layout: layouts/base.njk
---
<article class="sheet">
  <header class="sheet-head">
    <h1>{{ title }}</h1>
    {% if standfirst %}<p class="standfirst">{{ standfirst }}</p>{% endif %}
  </header>
  {{ content | safe }}
</article>
